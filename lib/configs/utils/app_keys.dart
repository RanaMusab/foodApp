/// Universal keys.
class AppKeys {
  static String appTitle = 'Food App';
  static String baseUrl = 'baseUrl';
}

/// `flutter_form_builder` field names, grouped per form.
/// One class per form keeps keys from colliding across screens.
class AuthFormKeys {
  static const String email = 'email';
  static const String password = 'password';
  static const String confirmPassword = 'confirm_password';
}

class HomeFormKeys {
  static const String search = 'search';
}
