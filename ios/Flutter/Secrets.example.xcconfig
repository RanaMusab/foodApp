// Copy this file to `Secrets.xcconfig` (gitignored) and add your own key.
// Debug.xcconfig and Release.xcconfig include it via `#include?`, so the
// build still works if it is missing — the map just renders blank.
GOOGLE_MAPS_API_KEY = ADD_YOUR_GOOGLE_MAP_KEY
